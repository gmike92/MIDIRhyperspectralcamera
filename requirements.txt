# Hyperspectral camera app (PyQt6). 64-bit Python 3.9-3.13.
# Pure-software / mock-mode dependencies (needed for `--mode mock`):
PyQt6
pyqtgraph
numpy
scipy
pandas
Pillow
loguru
# Optional, only for the real hardware (NOT needed for `--mode mock`):
#  - Hamamatsu DCAM-API + dcamapi runtime   (Orca Flash camera)
#      driven through camera/hamamatsu_backend.py -> ../Hamamatsu_ScopeFoundry/CameraDevice.py
#  - SmarAct MCS2 SDK  (smaract.ctl / SmarActCTL.dll)   (TWINS wedge, SLC-2460)
#      pip package: `smaract` (from the SmarAct MCS2 software installer)
#  - Thorlabs Kinesis + pythonnet           (optional delay stage + rotator, KDC101)
